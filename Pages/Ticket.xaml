<Page x:Class="Airlines_Toshmatov.Pages.Ticket"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White"
      Title="Ticket">

    <Grid Height="450" Width="700" MinHeight="450" MinWidth="800">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Background="#FF0C73FF" Height="60" VerticalAlignment="Center" HorizontalAlignment="Left" Width="800">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="371*"/>
                <ColumnDefinition Width="370*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="✈" Foreground="White" FontSize="24" VerticalAlignment="Top" Margin="15,16,11,0" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="19.717"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
            </TextBlock>
            <TextBlock Grid.Column="1" Text="Управление авиалиниями" Foreground="White" FontSize="18" VerticalAlignment="Center" Grid.ColumnSpan="2"/>
            <Button Grid.Column="2" Content="Вернуться" Width="100" Height="24" HorizontalAlignment="Left" Background="White" Margin="243,0,0,0" VerticalAlignment="Center" Click="Back"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="FlightsContainer" Margin="30,15,30,15">
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>